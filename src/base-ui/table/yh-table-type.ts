export interface showConfigType {
  showSerialNumber: boolean
  showSelection: boolean
  showPagination: boolean
}
export interface columArrItemType {
  type: string
  label?: string
  midWidth: string
  slotName?: string
}
